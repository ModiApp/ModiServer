module.exports = {
  // uniqueId(exclusions=[], len=5) {
  //   const randInt = () => Math.floor(Math.random() * 10);
  //   const randInts = (n) => n - 1 && randInts(n-1) + `${randInt()}`
  //   const id = randInts(len);
  //   if (exclusions.includes(id)) {
  //     return this._uniqueId(exclusions);
  //   }
  //   return id;
  // }
  uniqueId(s, v) {
    return '12345'
  }
}